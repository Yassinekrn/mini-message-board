extends layout

block content
  h1= title
  p Welcome to #{title}

  ul
    each val in messages
      li
        p= `${val.user} : ${val.text}`
        p= `${val.added}`

  button(onclick="window.location.href='/new'") Add a message to the board
